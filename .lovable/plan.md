## Ocultar bloco de depoimentos

### O que fazer
1. Abrir `src/routes/index.tsx`.
2. Localizar a linha `<Testimonials />` dentro do componente `SalesPage`.
3. Substituir por `{/* <Testimonials /> */}` para manter o componente comentado e pronto para reativar no futuro.

Nenhuma outra alteração será feita.